# Ejercicios en C


- **Ejercicio 1.** (src/ejercicios-c/e1.c): Escribe un programa en C equivalente a este programa en Python:
```python
print('Ingrese un valor entero')
val = int(input())
print('Salida:', val)
```

- **Ejercicio 2.** (src/ejercicios-c/e2.c): Escribe un programa en C equivalente a este programa en Python:
```python
print('Ingrese un nombre')
name = input()
print('Hola ', name)
```

- **Ejercicio 3.** Modifica el rograma `src/ejemplo1.c` de conversionde temperaturas e immprime un titulo a la tabla

- **Ejercicio 4.** Escribe un nuevo programa similar al del `ejemplo1.c` que genere una tabla de conversion de Celsius a Fahrenheit.

- **Ejercicio 5.** Escriba un programa para eliminar los espacios en blanco y las tabulaciones finales de cada línea de entrada, y para borrar las líneas completamente en blanco.

- **Ejercicio 6.** Escriba un programa para imprimir todas las líneas que tengan más de 80 caracteres.

- **Ejercicio 7.** Escriba una función `reverse(s)` que invierta la cadena de caracteres `s`. Úsela para escribir un programa que invierta su entrada línea por línea.


- **Ejercicio 8.** Escriba un programa que copie su entrada a su salida, reemplazando cada cadena de uno o más espacios en blanco por un solo espacio en blanco.

- **Ejercicio 9.**  Escriba un programa que reemplace cada tabulación por la secuencia de tres caracteres >, retroceso, -, que se imprime como >, y cada retroceso por la secuencia similar <. Esto hace visibles las tabulaciones y los retrocesos.

- **Ejercicio 10.**  Escriba un programa que imprima un histograma de las longitudes de las palabras de entrada. Es más fácil dibujar el histograma horizontalmente.
